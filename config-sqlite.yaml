# ATENCIÓN: Este archivo NO debe subirse a un repositorio git, en este caso se sube con fines didácticos y educativos

# Conexión a base de datos local con SQLite

source:
  driver: sqlite
  database: db.sqlite
